# macOS
.DS_Store

# Personal data exports — these should never be committed.
# Backups belong in iCloud Drive, not a public GitHub repo.
lift-hevy-import.json
lift-backup-*.json
*.csv

# Mac-only LaunchAgent + prune script. Local to your machine, not part of the
# web app deployed to GitHub Pages.
tools/
